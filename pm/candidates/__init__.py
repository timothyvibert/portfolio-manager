"""Options-scanner package: the on-demand roll/adjustment candidate layer.

Home for the scanner's own analytics — the vol-surface fit, IV+pp and IV-rank
(``surface``), and (later) candidate generation and ranking. Distinct from the
legacy ``pm.core.scanner_models`` (a dormant, unrelated experiment).
"""
